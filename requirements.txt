## Single-source requirements file tuned for running inside the repo venv
## - This pins a working set of packages used by the project and by the
##   Phase-1 analysis tooling (pyarrow, umap, fastparquet).
## - CUDA PyTorch wheels are installed from the official PyTorch CUDA 12.1 index.
##   Use Python 3.10 or 3.11 for the smoothest binary-wheel compatibility.

--extra-index-url https://download.pytorch.org/whl/cu121

# Core utilities
numpy==1.26.4
pandas==2.2.2
pyyaml==6.0

# Market data
yfinance==1.5.2

# Date & time handling
python-dateutil==2.8.2

# Machine Learning
# CUDA 12.1 build. If your machine has no compatible NVIDIA GPU, install the
# CPU-only PyTorch wheels separately instead of this requirements file.
torch==2.2.2+cu121
torchvision==0.17.2+cu121
torchaudio==2.2.2+cu121
scikit-learn==1.3.2

# Visualization
matplotlib==3.8.1
seaborn==0.12.2
umap-learn==0.5.4

# Testing
pytest==7.4.0

# Data serialization / parquet engines
pyarrow==14.0.1
fastparquet==0.8.2

# Utilities
tqdm==4.65.0

# Optional: add any additional analysis deps here
